# Internal Service System — operator env file
# Copy to .env and fill in real values. NEVER commit .env.

# Hostname of the CA host (the one running step-ca).
CA_HOST=hermes

# IP of the CA host. Used in cert SANs and Traefik config.
CA_IP=192.168.1.152

# Set true on the CA host itself (CA_HOST must equal `hostname -s`).
# Set false on every other host that joins the mesh.
JOIN_AS_CA=true

# Shared password users enter at http://enroll.local to get a cert.
HERMES_HOUSEHOLD_PASSWORD=REPLACE_ME

# .p12 install password. Mac users type this when importing the cert.
HERMES_P12_PASSWORD=hermes
