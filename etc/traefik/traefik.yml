entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"
providers:
  file:
    directory: <ISS_CONFIG_DIR>/dynamic
    watch: true
log:
  level: INFO
  filePath: /var/log/traefik.log
  format: common
accessLog:
  filePath: /var/log/traefik-access.log
  format: common
