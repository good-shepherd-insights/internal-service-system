{
  "_comment_1": "Map user identifiers to the service hostnames they may access. Traefik validates cert SANs against the connected hostname.",
  "_comment_2": "<CA_HOSTNAME> is replaced at install time with the value of CA_HOSTNAME in .env.",
  "anthony": ["<CA_HOSTNAME>.local"]
}
